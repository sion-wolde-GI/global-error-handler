# This is where placeholders for sensitive configuration data like credentials would go.
# Each sensitive parameter should be specified as an empty string. They also need to be
# specified as elements under the Cloudhub deployment properties in the pom.xml so that
# they can be injected via maven options.

otherSapi:
    client_id: ""
    client_secret: ""
    
backendService:
  username: ""
  password: ""
  